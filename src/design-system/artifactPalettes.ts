/**
 * Palettes for generated files that cannot resolve live CSS custom properties.
 * These values are intentionally outside the app chrome token graph.
 */
export const ERD_EXPORT_PALETTE = Object.freeze({
  canvas: "#15181d",
  edge: "#77808f",
  virtualEdge: "#9b8cff",
  nodeSurface: "#20242b",
  nodeBorder: "#414854",
  nodeTitle: "#f4f6f8",
  columnName: "#d5d9e0",
  columnType: "#8f98a7",
});
